/**
 * Property descriptor factories for big-endian numbers
 * @module
 */

/** @deprecated Import from `fields.ts` instead */
export {
  f16be,
  f32be,
  f64be,
  i16be,
  i32be,
  i64be,
  u16be,
  u32be,
  u64be,
} from "./fields.ts"
